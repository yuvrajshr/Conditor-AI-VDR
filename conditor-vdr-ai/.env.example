# --- Conditor VDR AI : server-side environment variables ---
# Get a FREE key at https://aistudio.google.com/apikey  (no billing required)
GEMINI_API_KEY=paste_your_free_gemini_key_here

# Optional: override the model (default gemini-2.5-flash).
# gemini-2.5-flash-lite is even cheaper/faster on the free tier.
GEMINI_MODEL=gemini-2.5-flash
